<section class="hero-cards">
  <div class="container">
    <div class="hero">
      <h1>Descubra Ilha Grande com quem vive aqui</h1>
      <p class="subtitle">Roteiros autênticos e dicas de especialistas locais no paraíso da Costa Verde.</p>
      <a href="/roteiros" class="cta-button">Ver Roteiros</a>
    </div>
    <div class="cards-section">
      <h2>Planeje sua Viagem</h2>
      <div class="cards-grid">
        <div class="card">
          <h3>O Que Fazer</h3>
          <p>Praias, trilhas e passeios de barco.</p>
        </div>
        <div class="card">
          <h3>Onde Ficar</h3>
          <p>Pousadas, hotéis e camping.</p>
        </div>
        <div class="card">
          <h3>Onde Comer</h3>
          <p>Culinária caiçara e frutos do mar.</p>
        </div>
        <div class="card">
          <h3>Guia Prático</h3>
          <p>Dicas e acessos à ilha.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    text-align: center;
    padding: 4rem 0;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    border-radius: 24px;
    margin-top: 2rem;
    color: white;
  }
  h1 {
    font-size: 2.5rem;
    margin-bottom: 1rem;
  }
  .subtitle {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    opacity: 0.9;
  }
  .cta-button {
    display: inline-block;
    background: var(--primary-green);
    color: white;
    padding: 0.75rem 2rem;
    text-decoration: none;
    border-radius: 8px;
    font-weight: 600;
    transition: background 0.2s;
  }
  .cta-button:hover {
    background: #059669;
  }
  .cards-section {
    padding: 3rem 0;
  }
  h2 {
    text-align: center;
    margin-bottom: 2rem;
    color: var(--dark-blue);
    font-size: 1.75rem;
  }
  .cards-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }
  .card {
    background: white;
    border: 1px solid #e2e8f0;
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
    transition: transform 0.2s, box-shadow 0.2s;
  }
  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 20px rgba(0,0,0,0.08);
  }
  .card h3 {
    margin: 0 0 0.5rem 0;
    color: var(--dark-blue);
    font-size: 1.1rem;
  }
  .card p {
    margin: 0;
    color: #64748b;
    font-size: 0.875rem;
  }
  @media (max-width: 768px) {
    h1 {
      font-size: 1.75rem;
    }
    .cards-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  @media (max-width: 480px) {
    .cards-grid {
      grid-template-columns: 1fr;
    }
  }
</style>